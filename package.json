{
  "name": "nexora-server",
  "version": "1.1.0",
  "description": "Nexora persistent API — OTP, merchant, products, orders",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=16"
  }
}
